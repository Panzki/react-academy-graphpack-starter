type Query {
  hello: String
  articles(searchText: String): [Article]
  users(limit: Int): [User]
  article(id: ID): Article
  user(id: ID): User
}

type Article {
  id: ID!
  title: String
  body: String
}

type User{
  id: ID!
  name: String
  email: String
  isEmployed: Boolean
  age: Int
}